import React, { Component } from 'react';

export default class ChatHeader extends Component {
   render() {
      return(
         <div id="chat-header">
            <h1>Chat Window</h1>
         </div>
      );
   }
}
